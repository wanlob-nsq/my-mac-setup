- name: Install Homebrew packages
  tags:
    - homebrew
  command: brew install "{{ item }}"
  with_items:
    - ack
    - asciinema
    - curl
    - git
    - postgresql
    - sqlite
    - tree
    - wget
